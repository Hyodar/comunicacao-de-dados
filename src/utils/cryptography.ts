
export default class Cryptography {
    static encrypt(text: Buffer): Buffer {
        // TODO

        return text;
    }

    static decrypt(text: Buffer): Buffer {
        // TODO

        return text;
    }
}
